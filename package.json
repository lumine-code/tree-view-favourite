{
  "name": "tree-view-favourite",
  "author": "lumine-code",
  "description": "Add files and folders to favourite sections in the tree view.",
  "repository": "https://github.com/lumine-code/tree-view-favourite",
  "bugs": {
    "url": "https://github.com/lumine-code/tree-view-favourite/issues"
  },
  "keywords": [
    "favorites",
    "bookmarks",
    "pinned",
    "shortcuts",
    "sidebar"
  ],
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "lumine": "^1.0.0"
  },
  "backgroundTips": [
    "{% if keys['tree-view-favourite:toggle'] %}You can show or hide your favourite files at the top of the tree view with {{ 'tree-view-favourite:toggle' | keystroke }}{% else %}You can pin the files and folders you use most into favourite sections at the top of the tree view.{% endif %}",
    "You can drop files onto a favourite section's header to pin them, and browse a pinned folder in place."
  ],
  "main": "./lib/main",
  "configSchema": {
    "defaultGroup": {
      "title": "Default group",
      "description": "The group new favourites are added to. A group with no entries in the current project is hidden, and one with no entries at all is dropped.",
      "type": "string",
      "default": "Favourites"
    }
  },
  "files": [
    "lib",
    "menus",
    "spec"
  ],
  "scripts": {
    "test": "lumine --test spec",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "consumedServices": {
    "tree-view.selection": {
      "versions": {
        "^1.0.0": "consumeTreeViewSelection"
      }
    },
    "tree-view.roots": {
      "versions": {
        "^1.0.0": "consumeTreeViewRoots"
      }
    }
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.2.2",
    "globals": "^17.9.0",
    "prettier": "^3.9.6"
  }
}
